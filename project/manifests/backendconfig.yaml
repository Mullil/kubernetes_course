apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: project-backendconfig
  namespace: project
spec:
  healthCheck:
    checkIntervalSec: 10
    port: 3001
    type: HTTP
    requestPath: /healthz